# Synthetic inference example

These sequence and coverage inputs are deterministic, synthetic, and included only to test the public inference path. They are not biological observations. The coverage values are already on the model input scale.
